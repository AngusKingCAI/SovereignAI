{
  "hooks": [
    {
      "name": "test-hook",
      "type": "command",
      "matcher": "",
      "events": ["SessionStart"],
      "command": "echo 'Hook triggered at SessionStart' > /tmp/hook-test.log",
      "description": "Test if hooks are working in current environment"
    }
  ]
}