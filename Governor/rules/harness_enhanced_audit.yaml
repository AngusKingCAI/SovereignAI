# Harness-Mode Rule: Enhanced Audit Requirements
# This rule applies only in harness mode for security auditing

id: harness_enhanced_audit
version: "1.0.0"
tier: blocking
agent: reviewer
domain: planning
scope: harness
name: Enhanced Audit Requirements
description: Requires enhanced audit logging for harness mode operations
triggers:
  - PreToolUse
  - PostToolUse
check:
  params:
    actions:
      - name: block_command
        reason: Enhanced audit required in harness mode
