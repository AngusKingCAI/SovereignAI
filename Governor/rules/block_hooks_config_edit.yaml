id: block_hooks_config_edit
version: "1.0.0"
tier: blocking
name: block_hooks_config_edit
description: Blocks editing of .devin/hooks.v1.json without explicit permission
triggers:
  - PreToolUse
agent: all
check:
  params:
    actions:
      - type: block_command
        params:
          reason: "Editing hooks.v1.json requires explicit permission"