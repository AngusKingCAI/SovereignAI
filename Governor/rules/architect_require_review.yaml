# Architect-Specific Rule: Enforce Architecture Reviews
# This rule requires architectural review for structural changes

id: architect_require_review
version: "1.0.0"
tier: blocking
agent: architect
domain: planning
name: Require Architecture Review
description: Requires architectural review for structural changes in App scope
triggers:
  - PreToolUse
check:
  params:
    actions:
      - name: block_command
        reason: Architectural review required for structural changes
