Depends on: Plan 18
Vision principles: P3 (no provider lock-in), P4 (local-first), P5 (wire as you go)
Open questions resolved: none (P4 minimum viable local — 2 adapters per session-context decision 12)

## S0 — Opening
- S0.1: Run /open (verify tag prompt-18 on origin)
- S0.2: Read AGENTS.md in full
- S0.2.5: Re-read AGENTS.md if S0.3 adds rules
- S0.3: Add OR70 (every adapter manifest declares `routing_priority` int — RoutingEngine routes ascending, lower = higher priority; default 1000 for manifests omitting the field; reserved range 0-999 for explicit priorities), commit before coding

## S1 — llama.cpp adapter scaffold
- S1.1: Create adapters/external/llama_cpp_adapter/ package: __init__.py empty.
- S1.2: Create adapters/external/llama_cpp_adapter/manifest.toml — component_id="llama_cpp_adapter", version="0.1.0", capabilities=["model.inference"], routing_priority=20.
- S1.3: Create adapters/external/llama_cpp_adapter/adapter.py — LlamaCppAdapter. Constructor args: trace, hardware_probe, model_path_resolver (Callable[[str], Path]), requested_n_gpu_layers: int = 0. ≤15 args (AR5). Lazy import: `import llama_cpp` ONLY inside health_check()/load_model()/generate(), never at module top level. __init__ performs no I/O. Store requested_n_gpu_layers as instance attr for health_check() to reference.
- S1.4: load_model(model_id: str) -> None: resolve path via self.model_path_resolver(model_id). Wrap file header read in try/except → raise AdapterUnavailableError("Invalid or unreadable GGUF file") on OSError. Read first 8 bytes; verify first 4 bytes == b'GGUF'; parse version as `int.from_bytes(buf[4:8], 'little', signed=False)` (little-endian uint32); accept version >= 2 (v2 and v3; v1 deprecated). Compute VRAM budget: `gpus = hardware_probe.sample().gpus; if not gpus: n_gpu_layers = 0; trace WARN "No GPU — CPU mode"; else: vram_budget_mb = max(g.vram_total_mb for g in gpus); n_gpu_layers = min(model.num_layers, vram_budget_mb * model.num_layers // max(1, model.vram_required_mb))`. Note: use max() not sum() — llama-cpp loads onto single device; multi-GPU tensor-split deferred. Store instance: `self._llm = llama_cpp.Llama(model_path=str(path), n_gpu_layers=n_gpu_layers)`. Emit trace on load start/complete/failure.
- S1.5: generate(prompt: str, max_tokens: int, temperature: float) -> str: wrap `self._llm.create_completion(...)` in try/except. On any exception: emit trace ERROR, raise `AdapterUnavailableError(detail=str(exc))`. Return generated text.
- S1.6: health_check() -> AdapterHealth: probe `import llama_cpp` in try/except. If import fails: return AdapterHealth(healthy=False, detail="llama-cpp-python not installed"). If import succeeds: adapter is healthy (can run in CPU mode). Separately probe GPU offload capability: if `self.requested_n_gpu_layers > 0`: check `hasattr(llama_cpp, 'llama_supports_gpu_offload')`; if absent: return AdapterHealth(healthy=False, detail="llama-cpp build predates GPU offload probe"); if present and `llama_cpp.llama_supports_gpu_offload()` is False: return AdapterHealth(healthy=False, detail="GPU offload not supported in this build"). If requested_n_gpu_layers == 0: skip GPU probe (CPU mode always available).
- S1.7: Add txt/requirements.txt: `llama-cpp-python>=0.2.50`. Add DEBT.md entry: "llama-cpp-python 0.2.50 — test with real binary before v1.0". Add DEBT.md entry: "generate() timeout — defer to post-Plan-19 (cross-platform timeout infra non-trivial)". Add DEBT.md entry: "health_check caching in RoutingEngine — defer to post-Plan-19 (TTL cache adds statefulness)". Add DEBT.md entry: "subscribe_callback bounded queue — defer to post-Plan-19 (per-subscriber queue + drain thread)". Add DEBT.md entry: "GPU capability probing is adapter-specific; future adapters must add their own probe in health_check() — defer AdapterCapability enum to post-Plan-19".
- S1.8: Edit adapters/external/ollama_adapter/manifest.toml (exists from Plan 7) — verify manifest does not already contain routing_priority; if absent, add `routing_priority = 10` field; if present, skip edit and log "already present".
- S1.9: Run /verify on each. Add tests/test_llama_cpp_adapter.py with llama_cpp mocked. Cover: load_model with no GPU (n_gpu_layers=0, vram_budget=0 path traced); load_model with multi-GPU (uses max not sum); load_model with corrupt GGUF (AdapterUnavailableError); load_model with v2 GGUF (accepted); load_model stores self._llm (generate() uses it); generate failure raises AdapterUnavailableError; health_check import failure; health_check GPU-offload failure when requested; health_check healthy when CPU-only (requested_n_gpu_layers=0); health_check healthy when offload probe absent but CPU mode.

## S2 — Model path resolver + RoutingEngine failover
- S2.1: Create sovereignai/shared/model_path_resolver.py — `default_model_path_resolver(model_id: str) -> Path` returns `Path.home() / ".sovereignai" / "models" / model_id.replace("/", "--")`. Register in build_container() (S3.1).
- S2.2: Edit sovereignai/shared/routing_engine.py — route(): (a) get adapters via capability_graph.adapters_by_capability(capability) sorted by routing_priority ascending; (b) filter out adapters where health_check().healthy is False (emit trace WARN "adapter {id} unhealthy — skipping"); (c) for each remaining adapter: retrieve instance via capability_graph.get_adapter(metadata.component_id); try call; on any exception, wrap in AdapterUnavailableError if not already; on AdapterUnavailableError, emit failover trace (old_adapter_id, new_adapter_id, capability) and try next; (d) if all exhausted: raise NoHealthyAdapterError. Single-adapter case: list of length 1 → try it → return result or propagate error (no silent fallback).
- S2.3: Edit sovereignai/shared/capability_graph.py — register() stores routing_priority from manifest; parser uses `manifest.get("routing_priority", 1000)` default for manifests omitting the field. Add method `adapters_by_capability(capability: str) -> list[ComponentMetadata]` sorted by routing_priority ascending. Add method `get_adapter(component_id: str) -> object` returning the registered adapter instance. Update manifest_parser.py to parse routing_priority field with default 1000.
- S2.4: Edit sovereignai/shared/types.py — add `AdapterUnavailableError` exception, `NoHealthyAdapterError` exception, `ComponentMetadata` dataclass (component_id, version, capabilities, routing_priority), `AdapterHealth` dataclass (healthy: bool, detail: str).
- S2.5: Run /verify on each. Extend tests/test_routing_engine.py: (a) failover test (Ollama raises AdapterUnavailableError, llama.cpp succeeds, verify failover trace); (b) single-adapter returns its result; (c) single-adapter raises → error propagates (no silent fallback); (d) all unhealthy → NoHealthyAdapterError; (e) health_check filter skips unhealthy adapter; (f) CPU-only llama.cpp (requested_n_gpu_layers=0) is healthy and routes successfully; (g) manifest without routing_priority defaults to 1000 and sorts last; (h) get_adapter() returns instance for route() to invoke.

## S3 — First-run experience
- S3.1: Edit sovereignai/main.py build_container() — after manifest loading, query CapabilityGraph for healthy model.inference adapters. If none: emit WARN trace "No inference adapter healthy — install Ollama or llama.cpp". Do NOT raise. Guard with `if not _test_mode` (existing pattern). Register default_model_path_resolver (from S2.1) for use by LlamaCppAdapter instantiation.
- S3.2: Edit web/main.py — add `GET /api/first-run-check` returning FirstRunStatusDTO (healthy_adapters: list[str], instructions: str).
- S3.3: WILL edit UI: web/templates/index.html — Models panel empty-state distinguishes "no models in DB" vs "no healthy adapter". web/static/app.js — loadModelsPanel() fetches /api/first-run-check, shows adapter-install prompt if healthy_adapters empty. web/schemas.py — add FirstRunStatusDTO.
- S3.4: Run /verify after each. Add tests/test_first_run_adapter_check.py covering: GET returns 200; empty healthy_adapters when both fail health_check; non-empty when at least one healthy; WARN trace emitted when none healthy; CPU-only llama.cpp counts as healthy.

## S4 — P4 compliance verification (mechanical)
- S4.1: Create scripts/ar_checks/check_p4_compliance.py — mechanical scan: (a) count adapters in adapters/external/ with manifest declaring capability "model.inference"; if <2, check DEBT.md for explicit P4 exception entry — must be a line starting with `P4-EXCEPTION:` followed by a target plan number (e.g. `P4-EXCEPTION: plan-99`); comment lines starting with `#` do NOT satisfy the gate; if no valid entry, FAIL; (b) verify RoutingEngine tests cover failover + single-adapter + all-unhealthy + CPU-only-healthy + manifest-without-routing_priority-default + get_adapter-returns-instance; (c) AST scan: find Raise nodes in main.py whose exception type is AdapterNotFoundError/ServiceNotFoundError/etc. and which are NOT inside `if not _test_mode` block — forbidden. Exit≠0 on violation.
- S4.2: Edit .devin/workflows/close.md step 8 — add check_p4_compliance.py to AR checks list.
- S4.3: Run /verify. Add tests/test_p4_compliance.py. Cover: passes with 2 adapters; fails with 1 adapter and no P4-EXCEPTION; passes with 1 adapter and valid `P4-EXCEPTION: plan-99` line; fails with commented `# P4-EXCEPTION` line; AST scan flags unprotected Raise.

## S5 — Vision compliance scorecard
- S5.1: In documents/plan-19-report.md (written at /close step 14.5), add "Vision Compliance Scorecard" section listing P1-P14 status (Active/Deferred/Violated) with evidence (file path or test name, NOT rule numbers — cite by content/filename to avoid numbering drift). Each Active principle must cite a passing test or implemented file. Deferred principles must cite DEBT.md entry.

## Closing
- Run /close (full suite, coverage ≥90% per OR43, all AR checks including check_p4_compliance.py, browser smoke test on first-run UX with screenshots to logs/screenshots/prompt-19/, spec_match.py, commit, tag prompt-19, push).
