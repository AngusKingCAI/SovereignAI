# Plan 30 — Scan Fix (Plans 26-29 Range Review)

**Type**: scan
**Scan range**: Plans 26, 27, 28, 29
**Depends on**: Plan 29
Vision principles: P9 (Observability by default), P13 (Strong and robust)
AR rules: AR4, AR8
OR rules: UOR-4, UOR-5, UOR-6, COR-3, SOR-1
Open questions resolved: none
**Revision**: Rev2

## Executor Manifest

**Plan**: 30
**Phases**: 7 (S0–S_close)
**Deliverables**:
| Phase | Deliverable | Verification |
|-------|-------------|--------------|
| S1 | `.agent/executor/OR_RULES.md` with SOR-1 added | `pytest .agent/executor/tests/ -k "rule" -v` passes |
| S2 | `.agent/shared/LANDMINES.md` with M7, M8 added | `pytest .agent/executor/tests/test_document_hygiene.py -v` passes |
| S3 | Fixed `check_messaging_no_department_manager_subclass.py` (portable paths) | Script runs successfully from any CWD |
| S4 | Fixed `check_messaging_whitelist_enforced.py` (portable paths) | Script runs successfully from any CWD |
| S5 | `verify_execution.py` updated with trace timestamp audit | `python .agent/executor/scripts/verify_execution.py --final --plan 29` still passes |
| S6 | CHANGELOG entry + PLANS.md update | N/A |

**Governance files**: MAY modify `OR_RULES.md` (add SOR-1) and `LANDMINES.md` (add M7, M8). Do NOT modify `AI_HANDOFF.md`, `AGENTS.md`, `PRINCIPLES.md`, `ARCHITECTURE.md`, `DEBT.md`.
**Commit message**: `scan-30: fix AR check paths, add SOR-1/M7/M8, range review 26-29`

## S0 — Opening

S0.1: Run `/open`
S0.2: Read `AGENTS.md` in full. Read plan header AR rules from `.agent/executor/ARCHITECTURE.md`. Read plan header OR rules from `.agent/executor/OR_RULES.md`.
S0.3: Check `.agent/shared/DEBT.md` for deferred items.

## S1 — Write SOR-1 to OR_RULES.md

S1.1: Add SOR-1 to `.agent/executor/OR_RULES.md` under `## SOR — scan Skill Rules`:

```
### SOR-1: Trace Timestamp Authenticity
**Rule**: Trace entries MUST be generated by `append_trace.py` hook at time of action. Entries with round-number timestamps (ending in :00:00.000) or sequential 1-second intervals in S_close indicate manual fabrication = STOP.
**Applies to**: scan skill, close skill
**Trigger**: `verify_execution.py --final` flags suspicious timestamps; manual review of trace files
```

S1.2: Run `pytest .agent/executor/tests/ -k "rule" -v` to verify no regressions.
S1.3: Run `/verify`.

## S2 — Write M7, M8 to LANDMINES.md

S2.1: Add M7 to `.agent/shared/LANDMINES.md` under `### Medium`:

```
| M7 | AR check scripts with hardcoded relative paths break across CWDs | `run_all.py` failures with path-not-found errors referencing drive root (e.g., `C:\app\...`) | All AR check scripts MUST use `Path(__file__).parent` chain for repo-root-relative paths. Audit existing scripts and refactor any using `Path.cwd()` or bare relative paths. |
```

S2.2: Add M8 to `.agent/shared/LANDMINES.md` under `### Medium`:

```
| M8 | Execution logs containing only headers (no body content) | `verify_close.py` line-count check on log files | Executor MUST paste full transcript. Empty/incomplete logs = STOP at `/close`. |
```

S2.3: Run `pytest .agent/executor/tests/test_document_hygiene.py -v` to verify landmine hygiene.
S2.4: Run `/verify`.

## S3 — Fix check_messaging_no_department_manager_subclass.py

S3.1: Read `.agent/executor/scripts/ar_checks/check_messaging_no_department_manager_subclass.py`.
S3.2: Replace all path resolution with `Path(__file__).parent.parent.parent.parent.parent / "app/sovereignai/messaging"` pattern. Ensure no `Path.cwd()` or bare `Path("app/...")` usage remains.
S3.3: Run `python .agent/executor/scripts/ar_checks/check_messaging_no_department_manager_subclass.py` — must exit 0.
S3.4: Run `/verify`.

## S4 — Fix check_messaging_whitelist_enforced.py

S4.1: Read `.agent/executor/scripts/ar_checks/check_messaging_whitelist_enforced.py`.
S4.2: Replace all path resolution with `Path(__file__).parent.parent.parent.parent.parent / "app/sovereignai"` pattern. Ensure no `Path.cwd()` or bare `Path("app/...")` usage remains.
S4.3: Run `python .agent/executor/scripts/ar_checks/check_messaging_whitelist_enforced.py` — must exit 0.
S4.4: Run `/verify`.

## S5 — Update verify_execution.py with Timestamp Audit

S5.1: Read `.agent/executor/scripts/verify_execution.py`.
S5.2: Add trace timestamp audit function: detect entries where timestamp seconds component is exactly `:00.000` (indicating round-minute fabrication). Flag these entries in output as `[WARNING]` with entry details. Do NOT FAIL on warnings — this is an audit signal, not a hard gate, since pre-compliance traces (Plans 26-27) don't exist and Plan 29 has legitimate ambiguity.
S5.3: Run `python .agent/executor/scripts/verify_execution.py --final --plan 29` — must still PASS.
S5.4: Run `/verify`.

## S6 — Documentation & Close

S6.1: Prepend CHANGELOG entry for scan-30 with scan range summary and findings.
S6.2: Update `PLANS.md`: set Plan 30 status to completed, clear queue.
S6.3: Run full test suite: `pytest .agent/executor/tests/ -x --tb=short`.
S6.4: Run `/close`. Tag: `scan-30`.
