agent: all
rules:
- id: SHR-01
  severity: blocking
  summary: Block destructive shell commands unless user has explicitly confirmed
- id: SHR-02
  severity: advisory
  summary: Use forward slash path separators, never backslash or absolute paths
- id: SHR-03
  severity: blocking
  summary: Every governance .md file must have YAML frontmatter with required fields
- id: SHR-04
  severity: blocking
  summary: All governance files must be placed in Governance/ directory, never in
    .devin/ or GovernanceScripts/
total_rules: 4
