{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SovereignAI Skill File Schema",
  "description": "Schema for validating SovereignAI skill markdown files",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "pattern": "^[a-z]+$",
      "description": "Skill identifier in lowercase"
    },
    "description": {
      "type": "string",
      "minLength": 10,
      "description": "Skill description"
    },
    "allowed-tools": {
      "type": "array",
      "description": "List of tools that are auto-approved for this skill",
      "items": {
        "type": "string"
      }
    },
    "triggers": {
      "type": "array",
      "description": "Trigger conditions for skill activation",
      "items": {
        "type": "string",
        "enum": ["user", "model", "file_change", "schedule"]
      }
    }
  },
  "required": ["name", "description"],
  "additionalProperties": false
}